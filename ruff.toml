target-version = "py311"
line-length = 100
extend-exclude = [
  ".git",
  ".venv*",
  "build",
  "artifacts",
  "paper_cpu_runs",
  "papers",
  "xsim.dir",
]

[lint]
select = ["E9", "F63", "F7", "F82"]
