# Project-scoped Codex config for NEMA
web_search = "cached"   # set to "live" only if you explicitly want it
sandbox_mode = "workspace-write"
approval_policy = "on-request"

[features]
multi_agent = true

# Optional: keep Codex focused
# (You can also enforce conventions via AGENTS.md)
